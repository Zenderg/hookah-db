{
  "name": "@hookah-db/services",
  "version": "1.0.0",
  "private": true,
  "description": "Business logic services for hookah-db",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "echo 'No linting configured for services package'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hookah-db/types": "workspace:*",
    "@hookah-db/utils": "workspace:*",
    "@hookah-db/scraper": "workspace:*",
    "@hookah-db/parser": "workspace:*",
    "@hookah-db/cache": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
