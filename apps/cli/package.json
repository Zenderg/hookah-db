{
  "name": "@hookah-db/cli",
  "version": "1.0.0",
  "private": true,
  "description": "CLI tool for hookah-db",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "hookah-db": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "echo 'No linting configured for cli app'",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hookah-db/types": "workspace:*",
    "@hookah-db/utils": "workspace:*",
    "@hookah-db/services": "workspace:*"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
