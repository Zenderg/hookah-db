/**
 * Rating distribution data model
 * Represents the distribution of ratings across different score levels
 */
export interface RatingDistribution {
  count1: number;
  count2: number;
  count3: number;
  count4: number;
  count5: number;
}
